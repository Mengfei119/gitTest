const a:number = 1;
const b:number = 2;

function add(a:number, b:number, c:number): number {
    //mxiong version 3
    return a+b-c;
};

function multi(a:number, b:number): number {
    return a* b;
}
